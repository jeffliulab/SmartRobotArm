# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.ipynb_checkpoints/

# ROS / colcon
build/
install/
log/

# Models & data (keep out of git; track with a model registry instead)
*.ckpt
*.pt
*.pth
*.safetensors
*.onnx
data/
datasets/
recordings/
*.bag

# Secrets / local config
.env
*.local

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Agent working notes (local only — not part of the open-source repo).
# AGENTS.md is deliberately NOT here: it is the public, machine-facing entry point.
CLAUDE.md

# Scratch + stray runtime logs (project rule: intermediates live in this repo's own temp/;
# the workspace root no longer has one — see the workspace CLAUDE.md, point 4).
temp/
MUJOCO_LOG.TXT
node_modules/
